# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Node environment
NODE_ENV="development"

# Better Auth
# Secret used by Better Auth
BETTER_AUTH_SECRET=""

# Better Auth GitHub OAuth
BETTER_AUTH_GITHUB_CLIENT_ID=""
BETTER_AUTH_GITHUB_CLIENT_SECRET=""

# Drizzle
DATABASE_URL="file:./db.sqlite"

# Opencode Server
# URL of the opencode server (set automatically in Docker)
OPENCODE_SERVER_URL="http://localhost:4096"

# Optional directory scoping for opencode serve (defaults to server's cwd)
OPENCODE_DIRECTORY=""

# Provider authentication for opencode
# Set these to configure AI provider credentials for opencode serve
# For Cerebras: set OPENCODE_PROVIDER_ID="cerebras" and use your CEREBRAS_API_KEY
# Get your API key from: https://inference.cerebras.ai/
OPENCODE_PROVIDER_ID="cerebras"
OPENCODE_PROVIDER_API_KEY=""  # Set to your CEREBRAS_API_KEY value

# AI Providers
# Cerebras API key for analysis and ranking tasks
CEREBRAS_API_KEY=""

# OpenRouter API key for summarization and writing tasks
OPENROUTER_API_KEY=""

# Ticket Providers
# Jira configuration
JIRA_BASE_URL=""
JIRA_API_TOKEN=""
JIRA_PROJECT_KEY=""

# Linear API key
LINEAR_API_KEY=""

# Docker configuration
DOCKER_SOCKET="/var/run/docker.sock"
OPENCODE_IMAGE="opencode:latest"

# Use the fast paid models for testing
FAST_MODE=false
